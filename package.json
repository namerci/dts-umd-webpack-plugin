{
  "name": "dts-umd-webpack-plugin",
  "description": "Webpack plugin for combining definition files to work with UMD modules",
  "version": "0.0.2",
  "main": "./index.js",
  "types": "./index.d.ts",
  "author": "Piotr Sałkowski <skc.peter@gmail.com>",
  "scripts": {
    "build": "rm -rf dist && tsc -p .",
    "prepareRelease": "cp -r ./dist/* ./ && rm -rf ./dist && rm -rf ./src && rm -rf ./tsconfig.json && rm -rf ./.github"
  },
  "dependencies": {
    "typescript-formatter": "^7.2.2"
  },
  "devDependencies": {
    "@types/node": "^14.0.4",
    "typescript": "^3.8.2"
  }
}
